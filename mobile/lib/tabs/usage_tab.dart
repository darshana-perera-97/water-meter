import 'package:flutter/material.dart';

class UsageTab extends StatelessWidget {
  const UsageTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('📊 Usage content'),
    );
  }
}
